{
  "name": "reidmitchell.net",
  "version": "1.0.0",
  "description": "my website",
  "main": "index.js",
  "repository": "https://github.com/reid47/website.git",
  "author": "Reid Mitchell <reidmitchell1@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "watch": "live-server --watch=\"src/*.css,src/*.html\" --mount=/:src",
    "build:clean": "rimraf dist",
    "build:static": "ncp static dist",
    "build:src": "ncp src dist",
    "build": "npm run build:clean && npm run build:static && npm run build:src",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^1.1.0",
    "live-server": "^1.2.0",
    "ncp": "^2.0.0",
    "postcss-cli": "^5.0.0",
    "rimraf": "^2.6.2"
  }
}
